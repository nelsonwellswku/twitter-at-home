overwrite: true
schema: "./schema.graphql"
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./schema.graphql.json:
    plugins:
      - "introspection"
